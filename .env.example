# Environment Configuration for NexusChat Flask App
# Copy this file to .env and fill in your actual values

# Flask Configuration
SECRET_KEY=your-super-secret-key-change-this-in-production-use-secrets.token_hex(32)
FLASK_ENV=production
DEBUG=False

# Google Gemini AI Configuration
GEMINI_API_KEY=your-gemini-api-key-here

# MySQL Database Configuration (for local development)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-mysql-password
DB_NAME=chatbot

# File Upload Configuration
UPLOAD_FOLDER=uploads
MAX_FILE_SIZE_MB=16

# Session Configuration
SESSION_LIFETIME_DAYS=30

# Tesseract OCR Configuration (if not in PATH)
# Windows: Uncomment and set path if needed
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe

# Linux/macOS: Usually in PATH, but can specify if needed
# TESSERACT_CMD=/usr/bin/tesseract

# Production Environment Variables (set these in your hosting platform)
# For Railway/Heroku/etc, set these in their dashboard:
# - GEMINI_API_KEY
# - SECRET_KEY  
# - DB_HOST
# - DB_PORT
# - DB_USER
# - DB_PASSWORD
# - DB_NAME
# - FLASK_ENV=production
# - DEBUG=False
